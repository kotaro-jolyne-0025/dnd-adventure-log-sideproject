<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.dndadvlog.backend.mapper.PasswordResetTokenMapper">

    <resultMap id="tokenMap" type="com.dndadvlog.backend.entity.PasswordResetToken">
        <id     property="id"         column="id"/>
        <result property="userId"     column="user_id"/>
        <result property="token"      column="token"/>
        <result property="expiryTime" column="expiry_time"/>
        <result property="usedAt"     column="used_at"/>
        <result property="createdAt"  column="created_at"/>
    </resultMap>

    <select id="findByToken" resultMap="tokenMap">
        SELECT id, user_id, token, expiry_time, used_at, created_at
        FROM password_reset_tokens
        WHERE token = #{token}
    </select>

    <insert id="insert">
        INSERT INTO password_reset_tokens (id, user_id, token, expiry_time, used_at, created_at)
        VALUES (
            #{id}::uuid,
            #{userId}::uuid,
            #{token},
            #{expiryTime},
            #{usedAt},
            NOW()
        )
    </insert>

    <update id="markAsUsed">
        UPDATE password_reset_tokens
        SET used_at = #{usedAt}
        WHERE id = #{id}::uuid
    </update>

    <update id="invalidateAllForUser">
        UPDATE password_reset_tokens
        SET used_at = #{now}
        WHERE user_id = #{userId}::uuid AND used_at IS NULL
    </update>

</mapper>
