<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.dndadvlog.backend.mapper.UserOAuthAccountMapper">

    <resultMap id="oauthAccountMap" type="com.dndadvlog.backend.entity.UserOAuthAccount">
        <id     property="id"             column="id"/>
        <result property="userId"         column="user_id"/>
        <result property="provider"       column="provider"/>
        <result property="providerUserId" column="provider_user_id"/>
        <result property="email"          column="email"/>
        <result property="createdAt"      column="created_at"/>
        <result property="updatedAt"      column="updated_at"/>
    </resultMap>

    <select id="findByProviderAndProviderUserId" resultMap="oauthAccountMap">
        SELECT id, user_id, provider, provider_user_id, email, created_at, updated_at
        FROM user_oauth_accounts
        WHERE UPPER(provider) = UPPER(#{provider}) AND provider_user_id = #{providerUserId}
    </select>

    <select id="findByUserId" resultMap="oauthAccountMap">
        SELECT id, user_id, provider, provider_user_id, email, created_at, updated_at
        FROM user_oauth_accounts
        WHERE user_id = #{userId}::uuid
        ORDER BY created_at ASC
    </select>

    <insert id="insert">
        INSERT INTO user_oauth_accounts (id, user_id, provider, provider_user_id, email, created_at, updated_at)
        VALUES (
            #{id}::uuid,
            #{userId}::uuid,
            #{provider},
            #{providerUserId},
            #{email},
            NOW(),
            NOW()
        )
    </insert>

    <delete id="deleteById">
        DELETE FROM user_oauth_accounts
        WHERE id = #{id}::uuid
    </delete>

</mapper>
